# 扩展情景（Expansion Roadmap）

auto-vision 的核心机制是 **`agent/request` 路由钩子 + 会话上下文检测 → 动态选择模型**。这个机制本身是通用的"按需路由"能力，视觉切换只是第一个应用。以下扩展情景按可行性分层。

## 第一层：直接可做（纯插件内实现，无需官方配合）

| # | 扩展 | 说明 | 优先级 |
|---|---|---|---|
| 1 | 多 provider 支持 | `targetProviders` 数组 ✅（v0.2 已完成） | P0 |
| 2 | 失败回退保护 | request-error 联动 ✅（v0.2 已完成） | P0 |
| 3 | 意图文本触发 | 用户说"看下这张图"即切 ✅（v0.2 已完成） | P0 |
| 4 | 审计统计 | 记录每次切换（原因/时间/模型），输出用量统计（JSON/日志） | P1 |
| 5 | 运行时开关 | `/auto-vision on\|off` 命令或 GUI 设置项，无需改配置重启 | P1 |
| 6 | 附件引用检测 | 检测消息中的 attachment 引用（不只是 image 块） | P1 |
| 7 | 误触抑制 | 意图检测加"否定词/技术讨论"上下文过滤（如"图片上传功能"类） | P1 |

## 第二层：需 DSH 官方 API/功能配合（或较大开发）

| # | 扩展 | 说明 | 优先级 |
|---|---|---|---|
| 8 | 模型升级矩阵 | 不止 vision：按任务复杂度自动 flash↔pro（复杂代码/长文档 → pro） | P1 |
| 9 | 上下文窗口感知 | 接近窗口上限时自动切更大 contextWindow 的模型，防截断 | P1 |
| 10 | 思考模式切换 | 需要深度推理时自动开启 reasoning（deepseek thinking 档位） | P2 |
| 11 | 子代理任务类型路由 | spawn 子代理时按任务类型自动选模型（standard-vision 预设思路泛化：任务→模型映射表） | P1 |
| 12 | workflow 阶段模型自动化 | workflow phase 声明所需能力，编排时自动选模型 | P2 |
| 13 | 用量/成本面板 | GUI 展示切换统计与 token 成本 | P2 |

## 第三层：生态联动（跨组件/跨会话）

| # | 扩展 | 说明 | 优先级 |
|---|---|---|---|
| 14 | 技能声明模型能力 | 技能 frontmatter 声明"需要 vision/大窗口"，触发技能时自动路由 | P1 |
| 15 | goal 阶段升级 | 长目标运行中按阶段/里程碑自动升级模型 | P2 |
| 16 | 跨会话偏好记忆 | 学习用户习惯（"该用户常发图"→ 会话默认 vision） | P2 |
| 17 | 多模态扩展 | 官方支持音频/视频后，同样机制扩展到对应模型 | P3 |
| 18 | 成本预算护栏 | 按会话/任务 token 预算自动降级（复杂→简单模型） | P2 |

## 推荐实施顺序

- **v0.3（P0 收尾 + P1 快赢）**：#4 审计统计、#5 运行时开关、#6 附件引用检测
- **v0.4（第二层起步）**：#8 模型升级矩阵（flash→pro 按任务复杂度）、#11 子代理任务类型路由
- **长期**：#14 技能联动（与 DSH 技能体系打通，技能声明能力→自动路由）

> 原则：任何"自动切换"都必须带**失败回退 + 可关闭**（v0.2 已建立该基线），避免模型选择变成新的故障源。
